# Environment Variables
# Copy this file to .env.local and fill in your actual values

# Polygon.io API Key (legacy, for signal evaluation)
POLYGON_API_KEY=your_polygon_api_key_here

# Massive Stocks Starter API Key (for calibration dataset)
# Get from: https://massive.com
# Features: Unlimited calls, 5yr history, 15-min delayed, daily bars
MASSIVE_API_KEY=your_massive_api_key_here
MASSIVE_BASE_URL=https://api.polygon.io

# Rate limiting: Massive has generous limits
# API provides: Stocks, ETFs, daily/minute bars, 5 year history

# Database
DATABASE_URL="file:./dev.db"

# Auth (for single-user setup)
AUTH_SECRET=generate_a_random_secret_here

# Agent Runtime - LLM Providers (at least one required for agent features)
# 
# FALLBACK ORDER: Providers are tried in AGENT_PROVIDER_ORDER.
# On quota/rate-limit (429, RESOURCE_EXHAUSTED, "quota", "rate limit"), 
# the runtime automatically falls back to the next provider.
#
# Default order: xai,openai,gemini,anthropic
# Override with comma-separated list:
AGENT_PROVIDER_ORDER=xai,openai,gemini,anthropic

# Cooldown (seconds) after quota/rate-limit before retrying a provider
# Default: 900 (15 minutes)
AGENT_PROVIDER_COOLDOWN_SECONDS=900

# xAI Grok (recommended first - fastest)
XAI_API_KEY=your_xai_api_key_here
XAI_MODEL=grok-4

# OpenAI (fallback)
OPENAI_API_KEY=your_openai_api_key_here

# Gemini (fallback)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash

# Anthropic (fallback)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Alpaca Broker Integration (READ-ONLY sync, no auto-trading)
ALPACA_API_KEY=your_alpaca_api_key_here
ALPACA_API_SECRET=your_alpaca_api_secret_here
ALPACA_BASE_URL=https://paper-api.alpaca.markets
